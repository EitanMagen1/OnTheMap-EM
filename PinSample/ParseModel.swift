//
//  ParseModel.swift
//  OnTheMap-EM1
//
//  Created by Lauren Efron on 14/01/2016.
//  Copyright © 2016 Udacity. All rights reserved.
//

import Foundation

class ParseModel {
    
  //  func GetingStudentLocations( completionHandeler : ( sucess :Bool , error : String?) ,
    /*
    let request = NSMutableURLRequest(URL: NSURL(string: "https://api.parse.com/1/classes/StudentLocation")!)
    request.addValue("QrX47CA9cyuGewLdsL7o5Eb8iug6Em8ye0dnAbIr", forHTTPHeaderField: "X-Parse-Application-Id")
    request.addValue("QuWThTdiRmTux3YaDseUSEpUKo7aBYM737yKd4gY", forHTTPHeaderField: "X-Parse-REST-API-Key")
    let session = NSURLSession.sharedSession()
    let task = session.dataTaskWithRequest(request) { data, response, error in
        if error != nil { // Handle error...
            return
        }
        println(NSString(data: data, encoding: NSUTF8StringEncoding))
    }
    task.resume()
*/
    // func POSTing a StudentLocation
    

    // optional to Querying for a StudentLocation
    // optional PUTing (Updating) a StudentLocation
    



  }
